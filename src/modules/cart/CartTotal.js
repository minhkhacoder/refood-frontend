/** @format */

import React from "react";
import styled from "styled-components";

const CartTotalStyled = styled.div`
  width: 320px;
  padding: 12px;
`;

const CartTotal = () => {
  return <CartTotalStyled>CartTotal</CartTotalStyled>;
};

export default CartTotal;
